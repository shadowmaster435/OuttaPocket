{
  "schemaVersion": 1,
  "id": "outta_pocket",
  "version": "${version}",
  "name": "OuttaPocket",
  "description": "Adds and modernizes the scrapped, unused, and simply mentioned.",
  "authors": ["shadowmaster435"],
  "contact": {},
  "license": "AGPL-3.0",
  "icon": "assets/outta_pocket/icon.png",
  "environment": "*",
  "entrypoints": {
    "client": [
      "org.shadowmaster435.outtapocket.client.OuttapocketClient"
    ],
    "main": [
      "org.shadowmaster435.outtapocket.Outtapocket"
    ]
  },
  "mixins": [
    "outtapocket.mixins.json",
    {
      "config": "outtapocket.client.mixins.json",
      "environment": "client"
    }
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-language-kotlin": ">=${kotlin_loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
